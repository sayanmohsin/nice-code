# Java guidance

Apply the Google Java Style Guide, explicit ownership boundaries, constructor injection where
dependency injection is used, clear API contracts, validation, safe configuration, structured
logging, and tests appropriate to the boundary. Use `java.time`, preserve exception causes, and
avoid direct production console output.

Source: https://google.github.io/styleguide/javaguide.html
