# Rust guidance

Prefer ownership and type-system guarantees over runtime conventions. Keep error context intact,
bound concurrency, avoid blocking async paths, and use Cargo formatting, tests, and clippy as
compiler-adjacent enforcement.
