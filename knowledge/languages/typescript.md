# TypeScript and JavaScript guidance

Keep types explicit at trust boundaries, validate external input, preserve async dependency order,
and use project formatters and compiler settings as the source of truth. Keep logging contextual
and free of secrets.
