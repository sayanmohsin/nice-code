# React guidance

Keep state ownership explicit, preserve accessible interaction semantics, avoid unnecessary
render work, and validate behavior with focused component and browser-level tests where needed.
