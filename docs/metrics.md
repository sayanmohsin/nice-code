# Metrics

Nice Code avoids a single quality score. Useful evidence includes findings by
status, category, and file class; review and false-positive rates; performance
claims with measurements; and security or correctness checks that block CI.

Generate machine-readable metrics from a JSON report with the repository metrics
script. For new/resolved/repeated trends, compare separate reports or baseline
files over time rather than assuming those fields are present in every report.
