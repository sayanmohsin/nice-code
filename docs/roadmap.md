# Roadmap

Near-term work is deliberately practical: improve signal quality from representative projects; keep agent output compact and stable; expand baseline and monthly audit reporting; publish the validated Node-compatible npm package; and add framework-aware checks only when a recurring gap is demonstrated.

Nice Code should stay dependency-light, conservative, and easy to remove from a project. New checks must earn their place through useful evidence.
