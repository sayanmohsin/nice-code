# Nice Code skills

Skills are curated, versioned guidance for coding agents and human reviewers.
The registry is in `registry.json`; every entry must have a checked-in skill
file, source attribution, revision, license information, and a version.

Use `nice-code skills list` to inspect bundled skills and
`nice-code skills check` to validate the registry.
